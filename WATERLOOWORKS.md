# WaterlooWorks 本地简历匹配

## 使用

1. 仓库根目录运行 `./scripts/setup-matcher.sh`。脚本创建独立 Python 环境、安装 Chromium、构建前端，并尝试安装可选的本地嵌入模型（约 130 MB，只下载一次，之后离线运行）。需要 Python 3.10+、Node.js 20+；macOS 自带的 Python 3.9 不够，脚本会优先回退到 `/opt/homebrew/bin/python3`，也可设置 `SWW_PYTHON`。不想要嵌入模型就用 `SWW_NO_EMBEDDINGS=1 ./scripts/setup-matcher.sh`——服务照常工作，检索降级为 BM25，页面上会写明。

2. 运行 `./scripts/start-matcher.sh`，打开 <http://127.0.0.1:8765/waterlooworks>。

3. 上传 Markdown（.md / .markdown）、PDF 或 Word DOCX 简历，最大 10 MiB。PDF 最多 20 页；三种格式提取正文均不超过 200,000 字符（发送给模型时另有 40,000 字符上限，超出会报错，不静默截断）。DOCX 支持正文、表格、文本框和被正文引用的页眉页脚，跳过删除修订和显式隐藏文字；图片中的文字不会自动 OCR，加密文件和旧版 `.doc` 不支持。简历只在服务内存中解析，重启后需重新上传。

4. **检查左侧的「简历拆分」。** 上传后页面会列出识别到的每段经历（类型、时长、标题、技术栈）、在实际工作/项目中用过的技能、以及**仅出现在技能清单里**的技能，还有识别到的可用工期。这是后续所有匹配的输入——如果拆错了，排名一定不对。没有 API key 时用本地规则拆分，有 key 时由模型拆分并由代码核对每条经历的引用确实来自简历原文。

5. 点击打开登录浏览器，在弹出的独立 Chromium 内完成 WaterlooWorks 登录和学校双重验证，打开 Co-op Full-Cycle 职位列表。无需向工具输入密码。

6. 采集会确认并开启 `My Program`，只读取本专业岗位；无法确认开关状态时会报错停止，不会把入口页的空结果当作采集完成。其他筛选条件会保留，请先清除不需要的筛选并切到表格视图。24 小时内成功读取的详情按岗位 ID 复用，只补抓未完成、失败、新增或过期的部分；每读完一个岗位即写入本机数据库，停止后可继续。默认上限 100 页、3,000 个职位。

### 请求节奏

「请求间隔」是**距上一次请求**的最小值，不是每次动作之外额外空转的时间。这个区别是有代价的：旧版是固定睡眠，2 秒间隔配上 2 秒的详情加载，实际是每 4 秒一次请求而不是每 2 秒；现在页面加载本身花掉的时间会被算进间隔里。

间隔还会自己调整：

- 站点响应超过 5 秒，或返回 5xx → 间隔自动加宽（最多 8 倍）。响应变慢是 429 之前的预警，这样是在撞上限之前先退让。
- 恢复很慢（每次健康响应只收回 10%）。退让要快、恢复要慢——代价落在你的账号上时，这个不对称是对的。
- 收到 429 → 直接停止采集，不重试。
- 每次等待带 ±15% 抖动，避免恒定节奏正好卡在某个固定速率窗口上。
- 图片、CSS、字体不计入速率，它们是浏览器在补全一个你已经请求过的页面。

默认 1 秒，下限 0.5 秒。合成基准上（站点每次响应 0.8 秒，30 个岗位）：

| 配置 | 每个岗位 | 实测请求速率 |
|---|---|---|
| 旧版固定睡眠 2 秒 | 3.22 s | 0.34 req/s |
| 间隔 2 秒 | 2.87 s | 0.38 req/s |
| 间隔 1 秒（默认） | 1.87 s | 0.59 req/s |
| 间隔 0.5 秒（下限） | 1.35 s | 0.81 req/s |

**这些不是 WaterlooWorks 公布的限制**——没有公开的数字。默认值是保守取值，即便踩到下限也仍在每秒一次请求以下。调低有账号风险，请自行斟酌。首次采集几千条详情仍需较长时间：3,000 个岗位在默认间隔下约 1.5 小时。

7. DeepSeek API key 放入根目录 `deepseek_api.txt`（只放 key，或 `DEEPSEEK_API_KEY=sk-...`），也可设置环境变量 `DEEPSEEK_API_KEY`。默认模型 `deepseek-flash`，可用 `DEEPSEEK_MODEL` 指定。服务只连接官方 `https://api.deepseek.com`。

8. 填写可选的目标岗位、地点、排除词，选择排序引擎并生成排名，然后下载含证据明细的 CSV。

9. 每条结果上都有「去申请 ↗」。它在已登录的浏览器里打开这个职位，不用重新登录。点过的职位标上「已打开过申请页」，方便在一百条里不丢位置。

   怎么打开取决于**这个职位有没有独立网址**：

   - **有地址**——在登录浏览器里新开一个标签直达，不动你正在看的列表页和筛选条件。登录浏览器没开时它就是一个普通链接。
   - **没有地址**（看板上多数职位）——标题是 `<a href="#" onclick="getPostingOverview('123456')">`，详情是就地弹出的 **dialog**，根本不存在可链接的地址。这时前端不给链接，而是请服务在登录浏览器里做采集器做的事：翻到那一页，找到 `dataViewerSelection` 等于该编号的行，点它的标题，等弹窗里能解析出这个编号。定位用的是行里的编号而不是位置，所以翻页、换筛选都不会点错。翻页要发请求，因此这一步和采集共用同一个限速闸门；职位已经下架或被浏览器里的筛选挡住时会明说找不到，而不是默默停在看板上。登录浏览器没开时会提示先打开它，同时给一个「复制编号」做手动兜底。

   采集时如果某个职位是在弹窗或新标签中打开的，那一刻的地址是真实可用的，会被记下来——这类职位之后就走第一种。

   **这个按钮不会替你提交申请。** 申请要选简历包、有时要写求职信和回答雇主问题，提交之后不可撤回；批量自动投递也正是会让学校账号被标记的行为。工具把你送到申请页面为止。那个标记记录的是"你打开过"，不是"你投了"——服务无从知道你是否真的提交。

---

## 两种排序引擎

两个引擎跑的是同一条**级联**，只是本地引擎在花钱那一步之前停下：

```
确定性过滤   免费         ~700 个合格职位
     ↓
混合检索     免费·本地    ~700    BM25 + 语义最大相似度
     ↓
本地重排     免费·本地     150    cross-encoder 逐对精算
     ↓  ← 本地引擎到此为止
模型评估     计费           50    抽取要求 + 逐条核对证据
```

### 本地排序（不调用 API，不发送任何数据）

1. 确定性过滤：去重，排除明确关闭、已过期、命中排除词、命中身份限制、以及**最短工期超出你可接受范围**的职位。
2. 混合检索：BM25 词频检索 + 简历经历与职位内容的逐块语义最佳匹配（装了嵌入模型时）。已经抽取过要求的职位会按「岗位真正要求的每一条」来比，必需条件权重 2、优先 1。
3. 本地重排：cross-encoder 把简历摘要和职位原文放在一起读，给检索靠前的 150 个重新排序。相关度直接取模型输出的 sigmoid——它是按二分类交叉熵训练的，这个值就是它自己校准过的「相关概率」。
4. 组合：相关度 55 分、**经历新近度 15 分**、词表技能重叠 25 分、偏好项 10 分；**缺失的维度会归一，而不是计 0**。仅出现在技能清单、未用于任何经历的技能只算半分。

嵌入模型或重排模型没装时会自动降级，页面顶部和每个职位的警告里都写明用了哪些信号——弱一些的排序不会被当成强的报告。

### 语义排序（DeepSeek）

1. 同上的过滤 → 检索 → 重排（两个引擎调用的是同一份代码）。
2. 按「候选数量」取重排后的前 N 个送入模型，默认 50。填 0 表示全量评估。**结果里始终显示合格数、重排数、已评估数和未评估数**，部分结果不会被当成完整排名。
3. 对每个候选职位调用两次模型：
   - **抽取岗位要求**——只看职位文本，不涉及简历。每条要求必须带一段可核对的职位原文引用，按职位内容缓存，改简历不会作废。
   - **逐条判定**——给定结构化简历和编号要求列表，逐条给出 直接经验 / 可迁移 / 未体现 / 待确认 / 资格冲突，并引用简历原文。必须对每一条要求都给结论，不能为了让分数好看而漏掉硬性要求。
4. 代码核对每条引用：
   - 引用在简历原文中找不到 → 降为「待确认」，不计分。
   - 引用能找到、但落在技能清单或联系方式等**非经历段落** → 降为「待确认」，不计分。这一判断来自简历分段结果，不是对原文再做一次正则猜测。
   - 「资格冲突」只在必需的资格类要求上成立。
5. 确定性打分（模型不给分数）：直接证据系数 1，可迁移 0.6，未体现/待确认 0；必需条件权重 2，优先条件权重 1；技术/经验/领域基础权重 35/40/25，只在职位实际提出的维度内归一。**经历新近度预留 10 分**，填写偏好时另预留 10 分。明确资格冲突（含本地检出的工期冲突）分数上限 20。
6. 分差 ≤3 的相邻岗位会在 Top-100 边界和靠前位置做至多 6 组正反两次对比；两次结论一致才交换顺序，**分数不变**。

结果会显示**必需条件覆盖率**——共几条硬性要求、覆盖了多少。这是申请时真正要看的数字。

---

## 经历新近度

越近的经历权重越高——它既是你现在真正熟练的东西，也是你接下来更可能想做的事。这是一个**排序维度**，会进最终分数。

衡量的不是岗位新不新，而是**支撑这个岗位的是你哪段经历**：

- 每段经历按结束时间做指数衰减，两年半衰，下限 40%（旧经历权重变低，但永远不会归零——基础不会过期）。「Present」按进行中算，权重 1.0；日期读不出来的按中性 70% 算，不会被当成很新或很旧。
- 语义检索里，每条岗位要求会去找**最匹配**的那段经历；那段经历有多新，就是这个岗位的新近度。注意「哪段最匹配」是用**未加权**的相似度判断的，否则新经历会因为权重高而自动胜出，新近度就成了自我实现。
- 没装嵌入模型时退化为词汇重叠找最匹配的经历。
- DeepSeek 模式下，新近度来自**实际满足了各条要求的那些简历段落**，必需条件权重加倍。

两条边界：

- **没有匹配就没有新近度分。** 一个什么都没匹配上的岗位，不会因为"它的空匹配被归到最近的经历"而拿分。
- **不影响证据是否成立。** 三年前做过就是做过，不会因为久远被降级为「未体现」。模型的提示词里明确写了这一点，新近度只在代码里单独计分。

副作用：简历不更新的话，分数会随时间缓慢下降。这是诚实的——你的经历确实在变旧。

---

## 工期筛选

判据是**岗位能接受的最短工期**，不是它有没有提到过某个数字。最短工期超过你可接受的上限时，职位被**排除**，理由里附上岗位原文。

| 岗位原文 | 最短工期 | 上限 4 个月时 |
|---|---|---|
| An uninterrupted eight-month placement is mandatory. | 8 | 排除 |
| Work Term Duration: 8 month | 8 | 排除 |
| This is a 12 month position. | 12 | 排除 |
| An eight-month placement is preferred, but four-month placements are equally eligible. | 4 | **保留** |
| Four-month placement. | 4 | 保留 |
| （没写工期） | — | 保留 |

数字和英文数词（eight-month）都能识别。上限的来源，按优先级：

1. 申请偏好里的「可接受的最长工期」（API 字段 `max_term_months`）；
2. 留空则取**简历中写明的可用工期**，例如 "available for a four-month work term" → 4；
3. 两者都没有 → 不做工期筛选，页面不会假装筛过。

填 0 表示关闭此项。这条规则完全由本地代码判断，不消耗 API。

---

## 身份资格筛选

申请偏好默认开启「排除 SWPP／强制公民或 PR 身份要求的岗位」。两个引擎在评分前检查标题、描述、要求及采集元数据：SWPP（含 Student Work Placement Program 全称）按关键词排除；其他公民／永久居民要求仅在识别到明确强制措辞时排除。身份优先、非强制的措辞会保留。排除原因显示在「匹配方法与排除记录」，取消勾选后重新生成排名即可关闭。API 对应 `preferences.exclude_restricted_eligibility`，省略时为 `true`。这是文本规则，未识别的措辞仍需核对原始岗位。

---

## 本机数据

- `.sww/browser/`：专用浏览器会话。
- `.sww/sww.db`：SQLite，存职位、采集进度、模型调用缓存和嵌入缓存。目录权限 700，文件权限 600。删除即清空全部缓存。
- `.sww/embeddings/`：本地嵌入模型文件。
- `deepseek_api.txt`：仅由后端读取；优先 `DEEPSEEK_API_KEY`，其次 `DEEPSEEK_API_KEY_FILE`，再其次默认根目录文件。密钥不会发给浏览器、不会进入模型提示词、不会提交到 Git。
- 简历文件在内存中解析，提取的正文保留在服务进程内存中，不落盘。
- 语义排序会先尽力去除常见邮箱、电话和链接，再发送简历结构与经历原文、职位文本和偏好；姓名、学校、项目名仍可能识别个人，**不能视为匿名**。
- CSV 由你主动下载。以公式符号开头的职位文本会转为文本值，避免在 Excel 中被当作公式执行。

清除登录会话：先停止服务，再删除 `.sww/browser/`。也可以在页面上清空模型缓存。

---

## JSON 导入

可以导入自己保存的职位 JSON，用来重新排序或在采集器需要适配时继续使用：

```json
{
  "jobs": [
    {
      "id": "123456",
      "title": "Software Developer",
      "company": "Example employer",
      "location": "Waterloo, ON",
      "description": "Build Python services and SQL data pipelines.",
      "requirements": "Python, SQL, Git",
      "deadline": "2027-01-20 11:59 PM",
      "url": "https://waterlooworks.uwaterloo.ca/myAccount/co-op/full/jobs.htm?jobId=123456"
    }
  ]
}
```

格式示例，不是真实职位。最多 10,000 条、整个请求最多 12 MiB。`title` 必填，其他可省略；非空链接必须属于 WaterlooWorks HTTPS 域名。导入结果始终标记为完整性未经核实。

---

## 接口

服务只在 `127.0.0.1:8765` 运行，跨来源写请求受 Origin、Host 和 `X-SWW-Client: 1` 校验保护。

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/matcher-api/status` | 采集、AI、嵌入模型与缓存状态 |
| `POST` | `/matcher-api/resume` | 上传并拆分简历，返回摘要（不含正文） |
| `GET` | `/matcher-api/resume/profile` | 查看完整的结构化简历 |
| `POST` | `/matcher-api/browser` | 打开登录浏览器 |
| `POST` | `/matcher-api/crawl` · `/crawl/cancel` | 开始／停止采集 |
| `POST` | `/matcher-api/jobs/import` | 导入职位 JSON |
| `POST` | `/matcher-api/jobs/{id}/open` | 在登录浏览器中打开该职位的申请页（不提交） |
| `POST` | `/matcher-api/rank` | 本地排序（同步） |
| `POST` | `/matcher-api/ai/rank` · `/ai/cancel` · `GET /ai/result` | 语义排序（后台任务） |
| `POST` | `/matcher-api/cache/clear` | 清空模型调用缓存 |
| `GET` | `/matcher-api/export.csv` | 导出当前排名 |

---

## 开发与验证

```bash
.venv/bin/python -m pytest matcher/tests -q
npm --prefix frontend run build
npx --prefix frontend tsc --noEmit
```

开发前端：`npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173`，`/matcher-api` 会代理到本地匹配服务。

DOCX 使用标准库读取 OOXML，无需 Word 或额外依赖；限制 ZIP 解压大小与 XML 大小，不展开到磁盘、不访问外部链接、不执行宏。

离线测量排序质量（不需要 API key，不花钱）：

```bash
PYTHONPATH=matcher .venv/bin/python scripts/evaluate-deepseek.py \
  --fixture matcher/tests/fixtures/semantic_adversarial_cases.json
```

加 `--live` 会额外调用真实 DeepSeek 跑语义链路，使用独立的 `.sww/evaluation-cache/`。

---

## 边界

- 分数是申请优先级参考，**不是录用概率**，也未用真实录用结果校准。
- 合成 fixture 上的 NDCG 只说明这些回归用例有效，不代表真实职位上的准确率。要可靠比较，需要你自己人工标注一批真实职位。
- 引用存在不等于模型的解释正确：能力推断、要求的重要性、语义矛盾判断仍需人工核对。
- 词表技能识别只覆盖内置词汇表，用于解释和辅助打分，不再决定排名。
- 简历分段能处理常见排版和 PDF 抽取出的连续段落，但复杂多栏排版可能拆错——所以页面把拆分结果显示出来让你检查。
- 采集器参考公开的近期 WaterlooWorks DOM 记录，以合成列表、弹窗和分页测试验证。站点结构变化时会报告解析/分页失败，仍需在实际账号上校准。未获得你的登录和简历前，无法声称已验证真实职位或生成个人 Top 100。
