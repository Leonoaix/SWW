# Local configuration. .env holds SESSION_SECRET and any future credentials —
# it must never be committed. .env.example documents the keys instead.
.env
.env.*
!.env.example

# Dependencies and build output
node_modules/
dist/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Uploaded resumes (Milestone 8) — user data, stored on local disk only
uploads/

# WaterlooWorks: private browser session, cached jobs and local exports
.sww/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
output/
